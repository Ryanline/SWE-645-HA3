apiVersion: v1
kind: Service
metadata:
  name: {{ include "swe645-ha3.fullname" . }}-backend
spec:
  type: {{ .Values.backend.service.type }}
  selector:
    app: {{ include "swe645-ha3.fullname" . }}-backend
  ports:
    - protocol: TCP
      port: {{ .Values.backend.service.port }}
      targetPort: 8000
      nodePort: {{ .Values.backend.service.nodePort }}
